@model IEnumerable<POPS.Models.PoDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QTY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ITDESC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SUPLNO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QTY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ITDESC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUPLNO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.PONO.Trim(), iTCode = item.ITCODE.Trim() }) |
            @Html.ActionLink("Details", "Details", new { id = item.PONO.Trim(), iTCode = item.ITCODE.Trim() }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.PONO.Trim(), iTCode = item.ITCODE.Trim() })
        </td>
    </tr>
}

</table>
